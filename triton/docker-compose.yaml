services:
  triton:

    build:
      context: .

    container_name: triton-server
    ports:
      - "8001:8000"
    volumes:
      - ./model_repository:/models
    command:
      - tritonserver
      - --model-repository=/models
