apiVersion: v1
kind: Service

metadata:
  name: anomaly-inference-service

spec:
  selector:
    app: anomaly-inference-system

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer
