
apiVersion: v1
kind: ConfigMap

metadata:
  name: anomaly-inference-config

data:
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"

  TRITON_MODEL_NAME: anomaly_detector
  MODEL_VERSION_2_CANARY_PERCENTAGE: "0.05"
