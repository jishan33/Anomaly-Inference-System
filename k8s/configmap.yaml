
apiVersion: v1
kind: ConfigMap

metadata:
  name: anomaly-inference-config

data:
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"

  MODEL_ROLLOUT_MODE: "canary"   # stable | canary | shadow
  MODEL_CANARY_FRACTION: "0.05"

  ANOMALY_MODEL: "transaction_anomaly" # transaction_anomaly | volume_anomaly
