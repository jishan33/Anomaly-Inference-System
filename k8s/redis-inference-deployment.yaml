apiVersion: apps/v1
kind: Deployment

metadata:
  name: redis-inference

spec:
  replicas: 1

  selector:
    matchLabels:
      app: redis-inference

  template:
    metadata:
      labels:
        app: redis-inference

    spec:
      containers:
        - name: redis
          image: redis:7-alpine

          ports:
            - containerPort: 6379